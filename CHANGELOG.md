# 变更记录

## 1.4.0-stations（2026-09-15）

默认Web入口切换为四工位TCP v2数据接收器：电机螺钉记录工号与数量；包装箱记录工号、完整条码、LOGO与火焰标识OK/NG。完整条码只在服务器端与预录标准精确比对，保留历史标准快照，不再下发目标，不建立场次或测试轮次，不评分。

新增两项目分页界面、独立工位连接状态、NG/不匹配筛选、持久化去重、收发日志、CSV/JSONL导出与工位数据库备份。新数据使用独立的station-results.sqlite3；保留旧库与明确的--legacy-v1入口。此前Windows备份fsync修复仍保留。

新增64项测试；合计240项通过。真实HTTP和四个TCP连接完成40条检测与4次重试；9项离线DOM测试通过。真实浏览器直连被环境策略拦截，未完成端到端浏览器验证；未构建Docker或部署Oracle。

## 1.3.0-web（2026-09-14，源码交付）

新增 FastAPI / Uvicorn 纯 Web 入口、中文裁判后台、认证会话、CSRF / Host / Origin 校验、受限请求体和静态白名单。复用原 TCP 服务、比赛状态机和 SQLite schema v2；现有 Windows 桌面入口保留。

新增单实例无桌面运行、后台维护队列、在线备份和导出下载、历史只读分页、操作请求去重与旧页面状态令牌、网页标题设置、TCP 监听控制。未知发送结果、超时和中断不改写成 NG。

新增 Dockerfile、Compose、凭据初始化工具、非 root 运行、只读根目录与数据目录挂载、健康检查和 Nginx 示例。没有硬编码容器 CPU 架构，目标为原生 ARM64/AMD64 构建；本次未实际构建这两种镜像。

新增 51 项单元/API/真实 TCP 测试，全部测试合计 176 项；另外提供独立 HTTP/TCP 冒烟、浏览器端到端和离线渲染脚本。浏览器直连本次因环境策略未完成。

未增加实时图像、自动评分、多赛道、多账户、TCP TLS；未推送 GitHub、未发布镜像、未修改 Oracle 服务器。旧版桌面业务版本保持 1.2.0-db。

## 1.2.0-db（此前版本）

正式/练习分类、schema v2、在线备份、恢复、新数据库管理窗口、导出锁隔离、目标版本历史与大屏状态口径修正。原测试报告仍保留为历史记录，不等同于本次 Docker 验收。
