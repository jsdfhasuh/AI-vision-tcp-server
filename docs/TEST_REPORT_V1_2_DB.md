# v1.2.0-db 测试报告

> 原交付包的历史测试报告：下文中的原始截图、JSON、ERR 和运行输出位于对应交付压缩包，不在 Git 中跟踪。本次入库复验见 [仓库导入说明](REPOSITORY_IMPORT.md)。

日期：2026-09-05。范围：数据库增强、既有TCP流程、裁判界面和公共展板关联修正。

## 环境

- 系统：Linux 6.18.35；不是Windows。
- Python：3.13.5。
- SQLite：3.46.1。程序会显示官方WAL-reset修复范围提示；本次测试不是该底层问题的专项复现，不能以通过测试证明任何运行时绝无损坏风险。
- 浏览器：Chromium 144.0.7559.96。
- Tk界面：Linux / Xvfb。测试用到Playwright/Pillow；这些不是服务端运行依赖。

## 自动化回归

运行：`python -m unittest discover -s tests -p "test_*.py" -v`

**125项通过：保留原有95项，新增30项数据库相关测试。** 本次原始输出见 `../tests/test_run_v1_2_db.txt`。

新增覆盖包括：v2应用标识/结构、用途锁定与筛选、分页和字面关键词、设置一次性导入、目标版本历史、结果不可覆盖、回执失败事务回滚、只读快照、慢速CSV导出不占用结果处理锁、已提交结果在线备份、校验失败拒绝恢复、只恢复到新目录、未完成轮次中断恢复、未来/未知/损坏数据库拒绝、迁移失败回滚及锁资源释放。

慢导出测试使用受控线程事件卡住CSV写入；在导出仍等待期间，结果能落库并返回正常接收状态。导出快照仍保留测试开始时的一致数据。它验证Python锁范围隔离，不是Windows/U盘真实性能或硬实时保证。

## 真实旧版代码创建数据库后的升级

使用本对话v1.1压缩包中的原始 `Store/Engine` 创建演示旧库，3份已接收结果和1轮未完成测试，然后由新版打开升级。

结果：schema 1→2，1个场次、4个轮次、3个回执保留；轮次ID和最终结果不变；未完成轮次变为INTERRUPTED；旧场次用途为LEGACY；审计18→19（新增恢复事件）；升级前备份仍是schema1，升级后完整性检查通过。

证据：`../tests/actual_v1_1_upgrade.json`。可复现工具：`../tools/verify_legacy_upgrade.py PATH_TO_UNPACKED_V1_1`。

## 裁判窗口 + 独立模拟客户端

实际启动Tk窗口与独立客户端子进程，包装箱、螺钉各10轮；每项收到10份结果、与预设一致10份，公共HTTP结果计数均为10，未发现Tk回调异常。

证据：`../tests/gui_smoke_v1_2_db.json`。截图：`screenshots/referee_v1_2_db.png`。截图中的队名、接入码和样件都是测试演示信息。

## 数据库管理窗口

在实际Tk窗口创建3场模拟数据，查询全部3场、筛选正式1场；选择历史场次后开始按钮禁用。通过界面调用完整性检查、手动备份，恢复后核对9轮记录。切场产生2份自动备份，正常退出后累计3份，最终备份完整性正常，Tk回调异常列表为空。

证据：`../tests/database_gui_v1_2_db.json`。截图：`screenshots/database_console.png`。这验证界面入口和数据库操作，不是Windows对话框路径编码/DPI验收。

## 前端回归

浏览器直接导航本机服务被测试环境以 `ERR_BLOCKED_BY_ADMINISTRATOR` 阻止。证据：`../tests/browser_direct_v1_2_db.err`。未把它计为直接网络验收成功。

使用原HTML/CSS/JS内容加载并经Python真实HTTP请求桥接fetch完成 19 项检查，覆盖真实TCP上报→HTTP快照→DOM、用途标签、arm待结果文案、取消计数、超时、断线、过期状态、恢复、文本注入防护、深浅主题、全屏及4种视口。JavaScript异常列表为空。

证据：`../tests/browser_smoke_v1_2_db.json`。桥接测试没有完整验证浏览器直接HTTP/CSP/Windows网络链路。

## 未完成的现场验收

Windows实机、Windows EXE构建、实际相机/实物识别、远距离物理大屏、双屏DPI、磁盘满、突然断电、硬件故障、长时间压力测试尚未进行。自动化通过不能等价于赛场验收。

相机图片上传、独立连续测试批次、观众大字模式、完整赛事阶段、评分发布和排行榜不在本版实现范围内。
