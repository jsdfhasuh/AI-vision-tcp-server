# 展板截图输出目录

历史演示截图保存在原 `vision_competition_tcp_v1_2_db.zip` 交付包。仓库不跟踪自动生成的 PNG，避免将裁判窗口中的接入码或样件信息意外公开。

无需截图即可运行程序和 `preview_display.html`。开发时可通过 `tests/browser_smoke.py`、`tests/gui_smoke.py`、`tests/database_gui_smoke.py` 重新生成演示截图；这些验收脚本需要额外的 GUI/浏览器测试环境。
